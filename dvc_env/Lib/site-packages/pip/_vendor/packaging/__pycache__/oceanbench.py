from oceanbench._src.geoprocessing.dynamic import load_patch_ds
import matplotlib.pyplot as plt

ds = load_patch_ds(
    task="ose/natl60",
    split="test",
    patch_kwargs={"dim": {"time": 10, "lat": 128, "lon": 128}},
    data_vars=["ugos", "vgos"]
)

print(ds)
ds["ugos"].isel(time=0).plot()
plt.title("ugos at time=0")
plt.show()
